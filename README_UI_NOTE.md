# UI reference

This app follows the A-type minimal work-app UI standard: top brand header, three task cards (input/settings/process), result KPI cards, and download cards. The reference image is maintained in the ChatGPT project/library rather than required at runtime.
